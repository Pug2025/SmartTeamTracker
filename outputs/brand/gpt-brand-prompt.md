# Prompt to paste into GPT 5.6

(Attach `smart-team-tracker-brand-context.md` to the same message.)

---

You are a senior brand designer specializing in sports-tech and mobile-first products. I've attached a brand context document for **Smart Team Tracker**, a rink-side hockey stat-tracking app for coaches. Read it completely before doing anything — it defines the product, audience, naming rules, existing color language, personality traits, required assets, and hard constraints. Treat every constraint in it as non-negotiable, especially the naming rules (§2), the pure-black-background requirement, and the 32-pixel legibility test (§8).

Work in this sequence, pausing for my approval between phases:

**Phase 1 — Concepts.** Propose 3 distinct logo/brand directions. For each: a name for the direction, the core visual idea, why it fits the brand personality in §6 of the context doc, how it survives at 32 px, and a rough description or sketch of the icon and wordmark lockup. Make the three directions genuinely different (e.g., one rink-geometry-led, one stat/data-led, one typographic/monogram-led). Give me your recommendation and why. Then stop and let me pick.

**Phase 2 — Develop the chosen direction.** Produce the full logo system: primary horizontal lockup, stacked lockup, standalone icon, single-color white version, and dark-on-light fallback. Deliver the icon and wordmark as clean SVG code I can use directly, plus generated images so I can see them rendered on black. Show the icon mocked at 512 px, 192 px, and 32 px, and inside a maskable-icon safe zone.

**Phase 3 — Palette and typography.** Define the brand palette with hex values, mapping it onto the existing UI tokens in §5 of the context doc (say explicitly which tokens change, if any, and which stay). Recommend a wordmark typeface and a UI font pairing with a free Google Fonts option, with fallback stacks.

**Phase 4 — Applications.** Produce: (a) the 1200×630 Open Graph / social share image, (b) app icon exports at 512 and 192 with the maskable variant, (c) a favicon concept shown at 32 and 16 px, and (d) a described landing-page hero concept on dark.

**Phase 5 — Brand one-pager.** A concise usage sheet: clear space, minimum sizes, color rules, and 4–6 do/don't examples.

Throughout: everything must be shown on pure black (#000000) first. Vector-first — whenever an asset can be SVG, give me the SVG source, not just a raster image. If you're ever unsure whether something violates the context doc, ask instead of guessing. Start with Phase 1 now.
